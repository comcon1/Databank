# Experiment Metadata Template

# Global Metadata
#
# REQUIRED: at least one of ARTICLE_DOI / DATA_DOI. Validation rejects an entry
# that gives neither. DATA_REF does not satisfy this: it is free text, not a
# resolvable identifier, so an entry whose raw data has no DOI still needs an
# ARTICLE_DOI.
#
# Which one is given also decides what the generated `bioschema_properties`
# block says. The rule is data first -- cite the data, not the paper:
#   ARTICLE_DOI only -> citation and sameAs name the article
#   DATA_DOI only    -> citation and sameAs name the deposition
#   both             -> citation and sameAs name the DEPOSITION, and the article
#                       becomes isPartOf (the parent work), so neither is lost
# Example: DATA_DOI 10.18710/ETWNCU -> bioschema_properties.citation: [10.18710/ETWNCU]
#
# Both are bare DOIs (10.xxxx/yyyy), without a doi: prefix or a https://doi.org/ URL.
#
# DEPRECATED: a plain `DOI:` key. It was the original single DOI field and is not
# accepted any more -- the schema does not declare it and rejects unknown keys.
# Rename it to ARTICLE_DOI when it names the publication, or to DATA_DOI when it
# names a deposition of the raw data. (A simulation README.yaml is unaffected:
# DOI is the right key there, for the Zenodo deposition holding the trajectory.)
ARTICLE_DOI: string  # DOI of the original publication of the experimental data
DATA_DOI: string     # DOI of the dataset deposition with raw data
DATA_REF: string     # reference to a deposited dataset that has no DOI; not a DOI substitute

DATE: date #Date as YYYY-MM-DD of the dataset publication - can be retrieved from crossref or datacite

TEMPERATURE: number  # Temperature (K) of the experiment, strictly positive

# Membrane Composition dictionary of molar fractions of membrane phase
MEMBRANE_COMPOSITION: object
  # Example: 
  # POPC: 0.93
  # CHOL: 0.07


# Solution Composition dictionary of ion concentrations in the system 
SOLUTION_COMPOSITION: object
  # Example:
  # SOD: 0.5
  # CLA: 0.24
  # GLUCOSE: 0.1

# Additional Molecules dictionary of molecules which are not in the databank.
# One line per molecule, naming it and its concentration in free text.
ADDITIONAL_MOLECULES: object
  # Example:
  # TFA: trifluoroacetic acid, 0.1%

TOTAL_HYDRATION: number  # Total hydration of the system (water mass %), within (0, 100]
PH: string              # pH of the system (number or UNKNOWN)
PH_METHOD: string       # Method of pH setting or measurement (buffer / measurement)

# Reagent Sources description of lipid reagents. Source, purity, etc. 
REAGENT_SOURCES: object
  # Example:
  # POPC: Avanti Polar Lipids


SAMPLE_PROTOCOL: string  # Protocol for sample preparation (in free format, references are welcome).

#Required: one of NMR-Specific Metadata or X-ray Specific Metadata

# NMR-Specific Metadata
NMR:
  T_RF_HEATING: string  # Correction of the temperature according to RF heating
  INSTRUMENT: string    # Instrument name (including field)
  METHOD: string        # NMR method (e.g., "2H:QE") check https://databank.readthedocs.io/stable/schemas/experiment_metadata.html#nmr-specific-fields for options
  SIGN_MEASURED: string # Method of OP measurement "abbr1:abbr2" (see detailed explaining below)
  DETAILS: string       # Additional experiment details

# X-ray Specific Metadata
XRAY:
  SOURCE: string        # X-ray source name
  LAMBDA: number        # Source wavelength or range. Wavelength (and/or range) of the X-ray beam used (in Ångstroms)
  QRANGE: string        # Scattering detection range (Q-range). The accessible scattering vector range, typically given in 1/Å.
  DETECTOR: string      # Detector type (e.g., CCD camera, PILATUS, or other detector model).
  DISTANCE: number      # Distance to detector (m)
  DATATYPE: string      # Measurement data type. Batch mode or size-exclusion chromatography (SEC) mode.
  EXPOSURE: number      # Exposure time per frame. The total data acquisition time per measurement frame, usually given in seconds.
  FRAMES: number        # Number of frames
  SAMPLE_TYPE: string   # 'MLV', 'SUV', 'GUV', 'OS' (oriented sample), 'BIC'. The type of sample used, with definitions: MLV (multilamellar vesicles), SUV (small unilamellar vesicles), GUV (giant unilamellar vesicles), OS (oriented sample), BIC (bicelles).
